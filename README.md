# Bosun Silence Hider

Расширение для Chrome/Chromium-браузеров, которое уменьшает шум на дашборде Bosun и помогает быстрее работать с алертами.

Плагин работает поверх интерфейса Bosun в браузере и ничего не меняет на сервере: он анализирует DOM страницы, скрывает засайленсенные алерты только там, где это действительно удобно, добавляет визуальные подсказки в секции **Needs Acknowledgement**, упрощает копирование текста алертов, умеет автоматически обновлять страницу при простое и подавать звуковые сигналы о новых событиях.

## Что умеет плагин

### 1. Скрытие `silenced alerts` только в `Acknowledged`

Плагин находит на странице алерты, уже отправленные в silence, и скрывает их **только в секции `Acknowledged`**.

В секции **`Needs Acknowledgement`** silenced alerts **не скрываются** и всегда остаются видимыми.

Что входит в эту часть логики:

* поиск алертов с иконкой mute (`fa-volume-off`)
* скрытие таких алертов только в блоке **Acknowledged**
* сохранение видимости silenced alerts в **Needs Acknowledgement**
* сохранение состояния переключателя после перезагрузки страницы
* отдельная кнопка скрытия/показа silenced с счётчиком и возможностью перемещения

### 2. Усиленное визуальное выделение silenced alerts

Для засайленсенных алертов плагин добавляет дополнительную текстовую метку **Silenced** рядом со значком динамика.

Что это даёт:

* silenced alerts проще заметить визуально
* статус виден быстрее, чем по одной только иконке
* метка не мешает обычной работе с алертом
* метка не попадает в выделение текста мышью

### 3. Подсветка алертов в `Needs Acknowledgement`

Плагин помогает быстрее понять, какие алерты в секции **Needs Acknowledgement** уже обработаны, а какие ещё требуют внимания.

Используются два дополнительных визуальных состояния:

* **note-значок** — у алерта уже есть Note
* **warning-значок** — у алерта нет Note

Для родительских групп действует приоритет:

* если внутри группы есть хотя бы один дочерний alert старше порога и без Note — у parent показывается **warning**
* если внутри группы есть хотя бы один дочерний alert с Note и нет проблемных старых alert без Note — у parent показывается **note**

Это позволяет быстрее ориентироваться в больших пачках алертов.
Так же в реализации Bosun есть баг при котором запись Note не сохраняется. Плагин помогает понять когда Note не записался. 

### 4. Кнопка `Copy` у алертов

Рядом с текстом алерта добавляется кнопка **Copy**, которая копирует текст конкретного алерта в буфер обмена.

Поддерживаются:

* родительские алерты
* дочерние алерты

Пример того, что копируется:

critical: sms channel group dlv percent low in test - 0.00% / 0.00%

После нажатия кнопка кратко меняет состояние, чтобы было видно, что копирование выполнено.

### 5. Кнопка `Copy all` у parent-групп

У родительских групп с двумя и более дочерними алертами появляется кнопка **Copy all**.

Текущая логика версии 1.4:

* кнопка показывается у parent-групп, где счётчик алертов `>= 2`
* кнопка располагается справа, рядом со счётчиком вида `5 alerts`
* по нажатию копируются **все дочерние алерты группы**
* копируется только текст дочерних alert, без служебных элементов
* в текущей реализации кнопка работает только для **раскрытой** группы
* если группа не раскрыта, плагин показывает предупреждение: **«Внимание! Сначала раскрой и проверь!»**

Пример результата копирования:

critical: push certificate revoked in channel-push_apnshttp-prod_03, ru_* on pushsrv03
critical: push certificate revoked in channel-push_apnshttp-prod_01, ru_* on pushsrv01
critical: push certificate revoked in channel-push_apnshttp-prod_05, ru_* on pushsrv05

### 6. Улучшение выделения текста мышкой

Чтобы было удобнее мышкой выделять пачку алертов и копировать только полезный текст, плагин делает невыделяемыми служебные элементы интерфейса:

* кнопку **Copy**
* кнопку **Copy all**
* счётчик вида `5 alerts`
* возраст алерта, например `11d-ago`
* идентификатор алерта, например `#263970:`
* метку **Silenced**

Кроме того, плагин защищает от случайного раскрытия алерта в момент ручного выделения текста мышью.
Это уменьшает шум при копировании списка алертов и не даёт выделению «проваливаться» в раскрывшийся дочерний блок.

### 7. Автообновление страницы по простою

В версии 1.4 добавлен режим **автообновления страницы по бездействию**.

Плагин может автоматически перезагрузить главную страницу Bosun, если в течение заданного времени пользователь не проявлял активности.

Что входит в эту функцию:

* переключатель **Автообновление**
* настройка интервала простоя в секундах
* отображение обратного отсчёта до обновления
* быстрое отключение по клику на индикатор
* сохранение состояния в локальном storage

Особенности:

* логика применяется только на основной странице Bosun
* на страницах вида `/action` автообновление не выполняется

### 8. Звуковое оповещение о новых алертах

В версии 1.4 добавлены **звуковые уведомления** для новых alert в секции **Needs Acknowledgement**.

Что делает плагин:

* отслеживает появление новых alert относительно предыдущего состояния страницы
* не воспроизводит сигнал на все старые alert при первой загрузке
* различает тип сигнала по уровню важности события

Логика сигналов:

* для **critical / unknown** используется более заметный тревожный звук
* для **warning** используется более мягкий звук

Также предусмотрена защита от слишком частого повторного воспроизведения одного и того же события.

### 9. Автоматическое снятие `Notify` на странице действия

Для страницы **`/action`** в версии 1.4 добавлена отдельная защитная логика.

Плагин автоматически находит checkbox **Notify** и снимает его, чтобы уменьшить риск случайной отправки лишнего уведомления при работе с action-формой.

Особенности:

* работает только на странице действия по алерту
* учитывает особенности фронтенда Bosun
* использует надёжную последовательность событий, чтобы изменение корректно подхватывал интерфейс

## Зачем это нужно

Стандартный Bosun Dashboard часто перегружен:

* часть алертов уже засайленсена и не требует внимания прямо сейчас
* в `Needs Acknowledgement` сложно быстро отличить уже просмотренные алерты от тех, которые ещё никто не отметил
* при ручном копировании текста в выделение попадают служебные элементы интерфейса
* при работе с группами алертов не хватает быстрого копирования всей пачки дочерних событий
* при дежурстве приходится вручную обновлять страницу
* новые alert’ы можно пропустить, если взгляд не был в нужный момент на вкладке
* на странице действия легко случайно оставить включённым `Notify`

Плагин решает эти задачи:

* убирает визуальный шум от `silenced alerts` в **Acknowledged**
* оставляет silenced alerts видимыми в **Needs Acknowledgement**
* усиливает визуальную заметность silenced alerts
* помогает быстрее понимать состояние алертов
* упрощает копирование текста одного алерта
* добавляет быстрое копирование всей раскрытой группы дочерних alert
* делает ручное выделение текста заметно удобнее
* автоматически обновляет страницу при простое
* подаёт звуковой сигнал при появлении новых alert
* снижает риск случайного notify-действия на странице action

## Как это работает

Плагин подключается к страницам Bosun как content script и работает только в браузере.

Основная логика:

* анализирует DOM дашборда
* определяет, какие алерты засайленсены
* скрывает или показывает их через CSS-класс
* применяет скрытие только к секции **Acknowledged**
* не скрывает silenced alerts в секции **Needs Acknowledgement**
* добавляет дополнительную метку **Silenced**
* отслеживает изменения DOM через `MutationObserver`
* обновляет визуальные маркеры для `Needs Acknowledgement`
* добавляет кнопки **Copy**
* добавляет кнопки **Copy all**
* помечает служебные элементы как невыделяемые
* предотвращает случайное раскрытие алерта во время drag-выделения текста
* отслеживает активность пользователя для автообновления страницы
* отслеживает появление новых alert для звукового оповещения
* на странице `/action` автоматически снимает флажок `Notify`

## Поддерживаемые адреса

Текущая версия подключается только к этим адресам:

* `https://bosun.edna.ru/*`
* `https://bosun-test.edna.ru/*`

Если понадобится использовать плагин на других инстансах Bosun, нужно будет расширить список `matches` в `manifest.json`.

## Структура проекта

manifest.json
content.js
README.md
bosun_notification_alert_chime.wav
bosun_notification_soft_chime.wav

## Установка в Chrome / Edge / Brave / Opera

### Установка из распакованной папки

1. Скачайте проект или распакуйте архив с файлами расширения.
2. Откройте страницу расширений в браузере:

chrome://extensions/

Для Edge:

edge://extensions/

3. Включите **Режим разработчика**.
4. Нажмите **Загрузить распакованное расширение**.
5. Выберите папку, в которой лежат `manifest.json` и `content.js`.
6. Откройте или обновите вкладку Bosun.

## Использование

После загрузки страницы Bosun плагин начинает работать автоматически.

### Работа с silenced alerts

На странице действует логика скрытия silenced alerts:

* в **Acknowledged** они могут скрываться
* в **Needs Acknowledgement** они остаются видимыми

Дополнительно рядом с mute-иконкой показывается метка **Silenced**.

### Работа с Needs Acknowledgement

В секции `Needs Acknowledgement` рядом с алертами и группами могут появляться дополнительные значки:

* значок Note — у алерта уже есть Note
* warning-значок — алерт без Note

Это визуальные подсказки, они не меняют поведение самого Bosun.

### Копирование текста алертов

Рядом с текстом алерта отображается кнопка **Copy**.

Она:

* копирует текст конкретного алерта в буфер обмена
* не должна мешать раскрытию и сворачиванию групп
* не попадает в выделение текста мышкой

### Копирование группы алертов

У parent-групп с несколькими дочерними alert отображается кнопка **Copy all**.

Она:

* копирует тексты дочерних alert одной пачкой
* не включает служебную информацию
* в версии 1.4 работает для раскрытой группы
* при попытке копирования со свёрнутой группы показывает предупреждение

### Автообновление страницы

В верхней панели плагина доступен режим **Автообновление**.

Он:

* отслеживает бездействие пользователя
* перезагружает страницу после заданного интервала
* показывает обратный отсчёт
* позволяет быстро отключить функцию

### Звуковое оповещение

В верхней панели плагина доступен режим **Звуковое оповещение**.

Он:

* отслеживает появление новых alert в `Needs Acknowledgement`
* воспроизводит звук для новых событий
* использует разные сигналы для warning и critical/unknown
* не должен срабатывать массово на уже существующий список после первой загрузки

### Страница Action

На странице действия по alert плагин дополнительно пытается автоматически снять флажок **Notify**.

Это не меняет серверную логику Bosun, а только уменьшает вероятность случайного уведомления из UI.

## Настройка

Часть параметров можно менять прямо в `content.js`.

Обычно имеет смысл настраивать:

* положение служебных кнопок на странице
* интервал обновления данных
* debounce для реакций на изменения DOM
* порог времени, после которого alert без Note получает warning-значок
* тексты служебных кнопок и предупреждений
* визуальное оформление метки **Silenced**
* параметры автообновления
* параметры звукового оповещения

После изменения параметров нужно перезагрузить расширение на странице `chrome://extensions/` и затем обновить вкладку Bosun.

## Обновление расширения

Если вы изменили `content.js`, `manifest.json` или звуковые файлы:

1. Откройте страницу расширений.
2. Нажмите **Reload / Перезагрузить**.
3. Обновите вкладку Bosun.

## Ограничения

У плагина есть несколько важных ограничений:

* логика завязана на текущую DOM-разметку Bosun
* если в Bosun изменится HTML-структура, часть функций может перестать работать
* визуальные маркеры в `Needs Acknowledgement` зависят от структуры групп и дочерних alert-элементов
* логика кнопок **Copy**, **Copy all** и невыделяемых элементов тоже зависит от текущей разметки заголовков
* кнопка **Copy all** в версии 1.4 работает только для уже раскрытой parent-группы
* звуковые уведомления зависят от политики браузера и могут ограничиваться настройками автопроигрывания
* автообновление рассчитано на главный экран Bosun и не применяется ко всем типам страниц
* логика снятия `Notify` зависит от структуры страницы `/action`
* расширение не является официальной частью Bosun и работает как внешний UI-слой

## Безопасность

Плагин выполняет только клиентскую визуальную обработку страницы.

Он:

* не изменяет данные алертов на сервере Bosun
* не отправляет содержимое алертов наружу
* не добавляет и не снимает silence
* не создаёт Notes
* не изменяет acknowledgement-статусы

Что реально делает расширение:

* читает DOM страницы Bosun
* определяет засайленсенные алерты по иконке mute
* визуально скрывает или показывает их через CSS
* добавляет локальные визуальные метки и кнопки
* сохраняет локальные настройки в `chrome.storage.local`
* копирует текст алертов в буфер обмена только по нажатию кнопок **Copy** и **Copy all**
* воспроизводит локальные звуковые файлы расширения
* может перезагружать страницу по условию бездействия
* на странице `/action` может автоматически снять флажок `Notify`

То есть это инструмент визуальной фильтрации, подсветки и удобного копирования, а не средство управления самими алертами.

## Совместимость

Текущий `manifest.json` использует **Manifest V3**, поэтому расширение в первую очередь рассчитано на Chromium-браузеры:

* Google Chrome
* Microsoft Edge
* Brave
* Opera
* Vivaldi
* Яндекс Браузер

Работа в Firefox теоретически возможна только после отдельной проверки и, при необходимости, адаптации.

## Версии

### v1.0

Первая версия расширения включала:

* скрытие `silenced alerts`
* кнопку показа и скрытия засайленсенных алертов
* счётчик скрытых алертов
* сохранение состояния через `storage`

### v1.1

Версия добавила:

* визуальные значки для алертов с Note
* warning-значки для новых алертов без Note
* агрегирование состояния на уровне parent-групп в `Needs Acknowledgement`

### v1.2

Версия добавила:

* скрытие silenced alerts только в **Acknowledged**
* сохранение видимости silenced alerts в **Needs Acknowledgement**
* кнопку **Copy** для родительских и дочерних алертов
* улучшение UX при выделении текста мышкой
* запрет выделения для служебных элементов: `Copy`, `5 alerts`, `11d-ago`, `#263970:`

### v1.3

Версия добавила:

* метку **Silenced** рядом с mute-иконкой
* кнопку **Copy all** для parent-групп
* копирование всей раскрытой пачки дочерних алертов
* предупреждение при попытке `Copy all` на свёрнутой группе
* запрет выделения для кнопки **Copy all** и метки **Silenced**
* защиту от случайного раскрытия alert во время ручного выделения текста
* возможность переноса кнопки скрытия/показа silenced alert

### v1.4

Версия добавила:

* Встроен toolbar в интерфейс Bosun
* автообновление страницы по бездействию
* настройку таймера автообновления
* обратный отсчёт до автоматической перезагрузки
* звуковое оповещение о новых alert в `Needs Acknowledgement`
* разные звуки для warning и critical/unknown
* защиту от ложного массового сигнала при первой загрузке
* автоматическое снятие флажка **Notify** на странице `/action`
* сохранение новых пользовательских настроек в `storage`

## Текущие файлы проекта

### `manifest.json`

Описывает расширение, его версию, разрешения, список страниц Bosun, где должен запускаться `content.js`, а также подключаемые ресурсы расширения.

### `content.js`

Основная логика плагина:

* поиск и скрытие silenced alerts
* создание и обновление визуальных меток
* отслеживание изменений DOM
* визуальная маркировка alert-элементов в `Needs Acknowledgement`
* добавление кнопок **Copy**
* добавление кнопок **Copy all**
* улучшение выделения текста
* автообновление страницы
* звуковые уведомления
* обработка страницы `/action`

### `bosun_notification_alert_chime.wav`

Звуковой сигнал для более заметных новых alert.

### `bosun_notification_soft_chime.wav`

Мягкий звуковой сигнал для менее критичных новых alert.

## Идеи для следующих версий

* сделать настраиваемые пороги и правила для звуковых уведомлений
* сделать подсветку алертов с устравервшим Note в секции Acknowledged
